Установлен SqLite. Перед миграциями войти в терминале в корень проекта и запустить

`touch database/user-service.sqlite`

Потом

`php artisan migrate
`

После стандартное разворчивание

`php artisan serve
`

`npm run dev `

